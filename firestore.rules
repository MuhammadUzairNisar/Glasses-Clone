rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in users can read/write (app uses: categories, items, stock, customers)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}